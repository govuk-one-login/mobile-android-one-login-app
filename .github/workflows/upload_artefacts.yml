name: Lint / Unit test codebase

on:
  workflow_call:
    secrets: inherit

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  report:
    name: Generate and Upload build artefacts
    steps:
      - name: Bundle build reports
        id: bundleReports
        run: |
          zip -9 -r reports.zip **/build/reports

      - name: Upload build reports
        id: uploadBuildReports
        uses: actions/upload-artifact@v3
        needs: bundleReports
        with:
          name: build-reports
          retention-days: 14
          path: |
            reports.zip
